## Intro
1. bitbake is a build engine like make but instead it works on recipe files.
2. each recipe descripes how to fetch and build a SW componenet: i.e. program, a library or an image
3. the input of bitbake is collectively called metadata
4. metadata: includes configuration files, recipes, classes and include files
